box: golang
build:
  steps:
    - install-packages: 
      packages: gcc git
    - script:
      name: go get
      code: go get github.com/grafana/grafana && cd ~/go/src/github.com/grafana/grafana
    - script:
      name: setup and build
      code: go run build.go setup && go run build.go build

